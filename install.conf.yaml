- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zsh: zsh
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
