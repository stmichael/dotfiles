return {
  text = {
    default = '#${text}',
    active = '#${lavender}',
    urgent = '#${red}',
  },

  background = {
    default = '#${base}',
  },

  border = {
    active = '#${lavender}',
    inactive = '#${overlay0}',
  },

  state = {
    success = '#${green}',
    warning = '#${peach}',
    danger = '#${red}',
  },
}
