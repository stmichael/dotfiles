set -g @text-color "#${text}"
set -g @text-alternate-color "#${text}"
set -g @text-active-color "#${lavender}"
set -g @text-urgent-color "#${red}"
set -g @text-selected-color "#${base}"

set -g @background-color "#${base}"
set -g @background-alternate-color "#${mantle}"
set -g @background-active-color "#${base}"
set -g @background-urgent-color "#${base}"
set -g @background-selected-color "#${text}"

set -g @border-color "#${text}"
set -g @border-active-color "#${lavender}"
set -g @border-inactive-color "#${overlay0}"

set -g @module-1-text-active-color "#${base}"
set -g @module-1-background-active-color "#${blue}"
set -g @module-1-text-inactive-color "#${blue}"
set -g @module-1-background-inactive-color "#${base}"
