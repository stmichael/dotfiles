gui:
  theme:
    activeBorderColor:
      - '#${lavender}'
      - bold
    inactiveBorderColor:
      - '#${overlay0}'
    optionsTextColor:
      - '#${blue}'
    selectedLineBgColor:
      - '#${surface1}'
    cherryPickedCommitBgColor:
      - '#${surface1}'
    cherryPickedCommitFgColor:
      - '#${lavender}'
    unstagedChangesColor:
      - '#${red}'
    defaultFgColor:
      - '#${text}'
    searchingActiveBorderColor:
      - '#${yellow}'

  authorColors:
    '*': '#${lavender}'
