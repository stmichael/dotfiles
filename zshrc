. ~/.zsh/configuration.zsh
. ~/.zsh/aliases.zsh
. ~/.zsh/autocompletion.zsh
. ~/.zsh/history.zsh
. ~/.zsh/theme.zsh

# --------------------
# custom configuration
# --------------------

export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init - zsh)"
